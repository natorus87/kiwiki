apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kiwiki.fullname" . }}-secret
  labels:
    {{- include "kiwiki.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secretEnv }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
