{{- if .Values.traefikRoute.v3.enabled }}
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ include "kiwiki.fullname" . }}-v3
  labels:
    {{- include "kiwiki.labels" . | nindent 4 }}
spec:
  entryPoints:
    - {{ .Values.traefikRoute.v3.entrypoint }}
  routes:
    {{- range .Values.traefikRoute.v3.hosts }}
    - match: Host(`{{ . }}`)
      kind: Rule
      middlewares:
        - name: {{ include "kiwiki.fullname" $ }}-cors-v3
      services:
        - name: {{ include "kiwiki.fullname" $ }}
          port: {{ $.Values.service.port }}
          serversTransport: {{ include "kiwiki.fullname" $ }}-sse-v3
          responseForwarding:
            flushInterval: {{ $.Values.traefikRoute.sse.flushInterval | quote }}
    {{- end }}
{{- end }}
