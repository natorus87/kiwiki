apiVersion: v1
kind: Service
metadata:
  name: {{ include "kiwiki.fullname" . }}
  labels:
    {{- include "kiwiki.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "kiwiki.selectorLabels" . | nindent 4 }}
