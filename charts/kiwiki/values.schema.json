{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "replicaCount": {
      "type": "integer",
      "minimum": 1,
      "maximum": 1,
      "description": "kiwiki currently supports exactly one replica because sessions, OAuth, SSE and uploads are process-local."
    }
  }
}
